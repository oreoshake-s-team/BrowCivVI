<!--
Title: use a semantic / Conventional Commit (e.g. `feat(map): navigable river corridors`).
This PR will be squashed into a single commit on merge (see CLAUDE.md).
-->

## Summary

<!-- What does this change do, in one or two sentences? -->

Closes #

## Changes

<!-- Bullet the notable changes. Keep app-code edits ≤ ~150 lines (excl. tests/CSS/config);
     if larger, link the follow-up sub-issues this was split into (docs/design.md §9). -->

-

## Screenshots / recordings

<!-- For any UI/board/visual change, include before/after. Note that you visually
     verified in a headless browser where applicable. Delete if not UI-facing. -->

## Historical accuracy & citations

<!-- For content (cities, units, battles, events, dates, coordinates), cite sources.
     Geography is exact and enforced; dates are flavor (docs/design.md §10).
     Enter "N/A" if this PR touches no historical content. -->
